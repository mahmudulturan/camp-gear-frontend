import { FC } from 'react';
import Banner from '../../components/pages/Home/Banner/Banner';

const HomePage: FC = () => {
    return (
        <div>
            <Banner />
        </div>
    );
};

export default HomePage;